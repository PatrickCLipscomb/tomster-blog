<form>
  <div class="form-group">
    <select class="" >
      {{#each model as |tag|}}
      <option>{{tag.name}}</option>
      {{/each}}
    </select>
  </div>
  <button {{action 'addTagtoTomster'}}>Add</button>
</form>
